# دليل التثبيت والتشغيل

## 📋 متطلبات النظام

- Python 3.8 أو أحدث
- pip (مدير الحزم)
- متصفح ويب حديث

## 🚀 خطوات التثبيت

### 1. فتح Terminal/Command Prompt
اذهب إلى مجلد المشروع:
```bash
cd c:\Users\m.kalo\Downloads\2\worker_management
```

### 2. إنشاء بيئة افتراضية (موصى به)
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 4. تشغيل التطبيق
```bash
python run.py
```

ستظهر رسالة مثل:
```
 * Running on http://127.0.0.1:5000
 * Press CTRL+C to quit
```

### 5. فتح المتصفح
اذهب إلى: `http://localhost:5000`

## 👤 بيانات الدخول الأولى

- **اسم المستخدم**: admin
- **كلمة المرور**: password

## ⚠️ إجراءات أمان مهمة

1. **غيّر كلمة المرور فوراً** بعد أول دخول
2. غيّر `SECRET_KEY` في `config.py` قبل الإنتاج
3. استخدم قاعدة بيانات قوية في الإنتاج

## 🆘 حل المشاكل الشائعة

### المشكلة: "ModuleNotFoundError: No module named 'flask'"
**الحل**: تأكد من تثبيت المتطلبات:
```bash
pip install -r requirements.txt
```

### المشكلة: "Port 5000 is already in use"
**الحل**: استخدم منفذ مختلف:
```bash
python run.py --port 5001
```

### المشكلة: خطأ في الوصول إلى قاعدة البيانات
**الحل**: احذف الملف `worker_management.db` وأعد تشغيل التطبيق

## 📚 المجلدات الرئيسية

```
worker_management/
├── app/                    # تطبيق Flask الرئيسي
├── app/templates/         # قوالب HTML
├── app/static/            # ملفات CSS و JS
├── config.py              # إعدادات التطبيق
├── run.py                # نقطة البداية
└── requirements.txt      # المتطلبات
```

## 🔧 الأوامر المفيدة

### تفعيل البيئة الافتراضية
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### تعطيل البيئة الافتراضية
```bash
deactivate
```

### فحص المتطلبات المثبتة
```bash
pip list
```

### تحديث المكتبات
```bash
pip install -r requirements.txt --upgrade
```

## 📖 أول خطوات الاستخدام

1. **سجّل الدخول** باستخدام بيانات المسؤول
2. **أضف الأصناف** في قسم الإعدادات
3. **سجّل العمال** الجدد
4. **أضف نوبات عمل** للعمال
5. **تتبع الإنتاج والمبيعات**

## 🌐 تشغيل على شبكة محلية

لتشغيل التطبيق على أجهزة أخرى في الشبكة:

```bash
python run.py --host 0.0.0.0
```

ثم افتح في المتصفح: `http://YOUR_IP:5000`

## 🆘 الدعم والمساعدة

إذا واجهت مشاكل:
1. تحقق من السجلات في Terminal
2. تأكد من استيفاء جميع المتطلبات
3. حاول إعادة تشغيل التطبيق

---
**تم الإنشاء بنجاح! استمتع باستخدام النظام** 🎉
