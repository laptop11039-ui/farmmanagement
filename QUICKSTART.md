# Worker Management System - Quick Start

## البدء السريع

### الخطوة 1: تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### الخطوة 2: تشغيل التطبيق
```bash
python run.py
```

### الخطوة 3: فتح المتصفح
```
http://localhost:5000
```

## الوصول الأول

**اسم المستخدم**: admin
**كلمة المرور**: password

## الملاحظات

- غيّر كلمة المرور الافتراضية فوراً
- قاعدة البيانات تُنشأ تلقائياً في المرة الأولى
- المشروع يدعم اللغة العربية بشكل كامل

## أوامر مفيدة

```bash
# إعادة تعيين قاعدة البيانات
rm worker_management.db
python run.py

# تشغيل في وضع الإنتاج
export FLASK_ENV=production
python run.py
```

استمتع باستخدام نظام إدارة المزرعة! 🌾
