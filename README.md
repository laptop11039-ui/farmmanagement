# إدارة المزرعة والعمال - Worker Management System

نظام ويب متكامل لإدارة العمال والأقسام المختلفة للمزارع والمشاريع الزراعية.

## 🌟 المميزات الرئيسية

### 1. **قسم العمال والإدارة** 👷
- تسجيل العمال مع بيانات التواصل
- تتبع ساعات العمل (صباحي وبعد الظهر)
- تحديد موقع العمل (جبل/سهل)
- حساب الرواتب بالدولار والليرة اللبنانية
- إدارة السلفات والحسابات (مدين/دائن)

### 2. **قسم الإنتاج** 🌾
تتبع إنتاج المنتجات التالية:
- **الدراق**: رومستار، ديومندري، أوريون، ريدشليده، سارويال، رين صن، فيري كود
- **التفاح**: أبيض وأحمر
- **الخضروات**: تنوع من المحاصيل
- تسجيل الموقع (جبل/سهل)
- تتبع الكمية والتاريخ

### 3. **قسم المبيعات** 💰
- تسجيل عمليات البيع
- حساب الإيرادات بالعملتين
- عرض الأرباح والتفاصيل

### 4. **قسم الوقود والمحروقات** ⛽
- تسجيل استهلاك الوقود (مازوت، بنزين، ديزل)
- حساب تكاليف الوقود

### 5. **قسم الأدوية والأسمدة** 💊
- إدارة الأدوية والمبيدات
- إدارة الأسمدة والعناصر الغذائية

### 6. **قسم الاستهلاك** 📦
- تتبع استهلاك المواد والمنتجات

### 7. **قسم التقارير** 📈
- تقرير العمال والرواتب
- تقرير الإنتاج
- تقرير المبيعات والإيرادات
- إمكانية الطباعة والتصدير

### 8. **قسم الإعدادات** ⚙️
- إدارة أصناف المنتجات
- تحكم المسؤول في البيانات الأساسية

## 🛠️ التكنولوجيا المستخدمة

- **Backend**: Flask (Python)
- **Database**: SQLite (قابل للتطوير إلى PostgreSQL)
- **Frontend**: HTML5, CSS3, Bootstrap 5
- **JavaScript**: Vanilla JS + Bootstrap
- **Authentication**: Flask-Login
- **ORM**: SQLAlchemy

## 📋 المتطلبات

- Python 3.8 أو أحدث
- pip (مدير حزم Python)

## 🚀 التثبيت والتشغيل

### 1. استنساخ المشروع
```bash
cd worker_management
```

### 2. إنشاء بيئة افتراضية (اختياري)
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. تثبيت المكتبات المطلوبة
```bash
pip install -r requirements.txt
```

### 4. إعداد قاعدة البيانات
```bash
python
>>> from app import create_app, db
>>> app = create_app()
>>> with app.app_context():
...     db.create_all()
>>> exit()
```

### 5. إنشاء حساب مسؤول
```bash
python run.py
# قم بتشغيل الأمر التالي في terminal آخر
python -c "from app import create_app, db; from app.models import User; app = create_app(); ctx = app.app_context(); ctx.push(); user = User(username='admin', email='admin@example.com', is_admin=True); user.set_password('password'); db.session.add(user); db.session.commit(); print('Admin user created')"
```

### 6. تشغيل التطبيق
```bash
python run.py
```

سيعمل التطبيق على: `http://localhost:5000`

## 👤 حسابات الدخول الافتراضية

بعد الإنشاء، استخدم:
- **اسم المستخدم**: admin
- **كلمة المرور**: password

**تنبيه**: غيّر كلمة المرور في الإنتاج!

## 📁 هيكل المشروع

```
worker_management/
├── app/
│   ├── __init__.py          # تطبيق Flask
│   ├── models.py            # نماذج قاعدة البيانات
│   ├── routes.py            # المسارات والـ blueprints
│   ├── templates/           # قوالب HTML
│   │   ├── base.html        # قالب أساسي
│   │   ├── dashboard.html   # لوحة التحكم
│   │   ├── auth/            # صفحات المصادقة
│   │   ├── workers/         # صفحات العمال
│   │   ├── production/      # صفحات الإنتاج
│   │   ├── sales/           # صفحات المبيعات
│   │   ├── fuel/            # صفحات الوقود
│   │   ├── medicines/       # صفحات الأدوية
│   │   ├── consumption/     # صفحات الاستهلاك
│   │   ├── reports/         # صفحات التقارير
│   │   └── settings/        # صفحات الإعدادات
│   └── static/
│       ├── css/style.css    # الأنماط
│       └── js/main.js       # سكريبت جافا سكريبت
├── config.py                # إعدادات التطبيق
├── run.py                   # نقطة البداية
├── requirements.txt         # المكتبات المطلوبة
└── README.md               # هذا الملف
```

## 🔐 الأمان

- كلمات مرور مشفرة بـ Werkzeug
- جلسات آمنة
- حماية CSRF محتملة (يمكن إضافتها)
- التحقق من صلاحيات المسؤول

## 🎨 التخصيص

### تغيير ألوان الموقع
عدّل ملف [app/static/css/style.css](app/static/css/style.css) وغيّر متغيرات الألوان:

```css
:root {
    --primary-color: #0d6efd;
    --success-color: #198754;
    /* ... */
}
```

### تغيير اللغة
التطبيق بالكامل باللغة العربية. لتحويله إلى لغة أخرى، غيّر جميع النصوص في القوالب.

## 📊 حالات الاستخدام

### لصاحب المزرعة
- متابعة الإنتاج اليومي
- حساب رواتب العمال
- معرفة الأرباح والخسائر

### لمدير المزرعة
- تسجيل العمليات اليومية
- متابعة سير العمل
- إعداد التقارير

### للموارد البشرية
- إدارة قاعدة بيانات العمال
- حساب الرواتب والسلفات
- متابعة ساعات العمل

## 🐛 الأخطاء الشائعة

### لا يمكن الاتصال بقاعدة البيانات
تأكد من وجود ملف `worker_management.db` في المجلد الرئيسي.

### خطأ في استيراد المكتبات
تأكد من تثبيت جميع المكتبات:
```bash
pip install -r requirements.txt
```

## 📚 الإصدارات المستقبلية

- [ ] تصدير البيانات إلى Excel/PDF
- [ ] تقارير متقدمة وإحصائيات
- [ ] تطبيق موبايل
- [ ] API REST
- [ ] نظام إشعارات
- [ ] دعم عملات إضافية

## 📞 الدعم والمساعدة

للمساعدة أو الإبلاغ عن الأخطاء، يرجى:
1. فحص السجلات في terminal
2. تحقق من المتطلبات
3. اتصل بفريق التطوير

## 📄 الترخيص

هذا المشروع مفتوح المصدر وحر الاستخدام.

---

**آخر تحديث**: يناير 2026
**الإصدار**: 1.0.0
